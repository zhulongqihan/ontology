# Product

## Register

product

## Users

论文研究者、引擎开发者和需要复核运行事实的技术人员，在本地开发环境中管理模型、Schema、Workflow、本体关系和服务注册，并追踪一次运行从请求到持久化的证据链。

## Product Purpose

这是一个可运行的柔性引擎与本体化平台控制面。它把模型配置、版本、运行上下文、快照、Trace、审计和幂等记录放在同一条可追溯链路中，让使用者能够修改配置、执行真实运行、定位失败原因并导出可复核结果。成功标准是每个关键动作都有明确状态、版本绑定和可检查的持久化证据，而不是展示静态演示数据。

## Brand Personality

专业、克制、透明。界面应让复杂的配置和运行状态变得可读，但不使用夸大的成功叙事；错误、兼容边界和本地实现都应被明确标识。

## Anti-references

不做静态大屏或只在浏览器内存中变化的模拟控制台，不伪装成外部生产系统，不用重复的 SaaS 卡片、无意义的装饰动画或把错误隐藏在成功提示后的交互。

## Design Principles

- 事实优先：状态、版本、身份和错误直接来自真实引擎接口。
- 证据可追溯：运行详情要能沿 Run、Context、Snapshot、Trace、Audit 和幂等记录继续下钻。
- 变更有边界：危险或不可逆的语义变化要显式说明，失败不能悄悄推进上下文。
- 专家可读：保留模型 ID、版本、事件和哈希等技术信息，同时给出中文解释。
- 复核友好：加载、空状态、冲突、失败和重试都要有稳定且可访问的反馈。

## Accessibility & Inclusion

以 WCAG 2.1 AA 为目标：键盘可操作、焦点可见、状态不只依赖颜色、表格在窄屏可横向阅读，并尊重 `prefers-reduced-motion`。错误信息应与对应操作区域关联，长 ID 和哈希应可复制且不依赖颜色辨识。
