# 开发进度 v0.1

更新时间：2026-09-02

## 已完成

- 独立 Java/Maven 多模块工程。
- Java 8 兼容语法目标，可在当前 JDK 17 上构建。
- 柔性字段定义、动态记录、字段校验。
- 工作流状态、事件转换、非法事件拒绝。
- 上下文快照和稳定 SHA-256。
- schema 版本迁移、字段改名和默认值。
- Questionnaire、Subject、Option、OntologyRelation。
- `JobOntologyDetail` 的固定属性、动态属性和关系。
- 本地 Questionnaire Provider/Consumer 兼容层。
- 20 条契约规格文件。
- 20 条契约代码执行器。
- 每个用例的 request/response/trace/result JSON。
- 可独立运行的 fat jar。
- React/Vite 中文实验控制台：总览、契约矩阵、Trace Inspector、柔性引擎、本体模型、运行记录。
- 前端响应式布局、状态标签、证据身份标识和非法输入筛选。

## 当前可验证结果

- `mvn test`：6 个模块、13 个测试，全部通过。
- `mvn package`：构建成功。
- `java -jar reproduction-app/target/reproduction-app-0.1.0-SNAPSHOT.jar`：smoke demo 成功。
- `java -jar reproduction-app/target/reproduction-app-0.1.0-SNAPSHOT.jar contract`：20/20 通过。
- 相同 seed 的两次契约报告 SHA-256 一致：
  `252EB33F56B97FEFFBC723A327C95CE452FB8A4CB4993D21FF9D02A8A7E8E550`。
- `frontend/npm run build`：TypeScript 检查和 Vite 生产构建成功。
- 浏览器检查：总览、契约矩阵过滤、C-20 Trace 联动、本体关系页均可用；控制台 0 error / 0 warning。
- 已生成视觉检查图：`output/playwright/overview.png`、`output/playwright/ontology.png`。

## 尚未完成

- 真实数据库持久化和每次运行的数据库前后快照。
- 完整 Trace 计时和错误 span。
- 柔性流程的回滚、重试、幂等故障注入。
- 37 条本体行为场景的重建执行器。
- 历史 R5 XML/日志导入器。
- REST API 和中文实验控制台。
- 前端从运行目录直接读取 JSON，而不是使用当前的只读展示快照。
- 论文实验章节的替换和新数据审查。

## 数据口径

当前 `runs/contract-20` 下的数据是本项目代码实际执行产生的 `REPRODUCED_SYSTEM_RUN`，不是原生产系统数据，也不是手工填写的模拟结果。

当前 `frontend/src/data.ts` 是上述运行结果的只读可视化投影，用于提前检查交互和信息架构，不作为新的实验数据源；正式报告仍以 `runs/contract-20` 中带哈希的运行产物为准。
