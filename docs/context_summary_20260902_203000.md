# 上下文压缩摘要（2026-09-02）

## 当前目标

对 `F:/finalartical/flexible-ontology-reproduction` 进行系统复现审查：核对代码、测试、运行产物、SQLite/JSON、架构与需求文档、论文原稿；修复可补齐的工程/测试/实验/论文缺口；完成验证、提交并推送。

## 已完成的状态核对

- 初始分支为 `main`，初始 HEAD 为 `0307f3d feat: add explainable audit diffs and safe sqlite restore`，工作区最初无未提交改动。
- 项目内未发现 `current_state.json`、`events.jsonl` 或既有状态审计脚本；当前 SQLite 是主要持久化事实来源，JSON 是兼容/快照视图，runs/output 为派生产物。
- 论文原稿位于 `F:/finalartical/毕业论文工作区/06_论文写作产物/07_论文源文件/njuthesis_正式初稿/`，不属于该 Git 仓库。

## 已完成的工程修复

1. 兼容适配层增加显式 request-boundary 方法，支持 legacy/current 字段映射并保留原始请求形状。
2. 契约实验 runner 真实解析 CSV 的 `request_shape`，修复只改标签而未发送 legacy 请求的问题。
3. 运行请求哈希纳入 ontology 输入；运行时深拷贝嵌套输入，避免 DTO 可变性污染持久化证据。
4. 非法 `unknownFieldPolicy` 改为拒绝，不再静默回退到 `REJECT`。
5. 旧运行记录保留 `REPRODUCED_SYSTEM_RUN`，证据不完整记录显式标记为 `LEGACY_RUNTIME_RECORD`；旧本地 Provider 名称规范化为 `LocalOntologyProvider`。
6. SQLite 对当前形状的坏运行投影/缺失 Trace 拒绝，只允许明确旧形状进入 legacy 兼容路径。
7. 本体 exact `1:1` 关系补齐零目标校验；Schema 版本发布拒绝跳号。
8. Admin API CORS 补充 `If-Match` 与 `ETag`/`X-Trace-Id` 暴露。
9. 前端改为使用真实 `spanId`、审计 revision/change 信息及 API 错误协议字段。
10. 新增只读状态一致性审计脚本 `scripts/audit_project_state.py`，新增最终契约 runner 的 source revision 注入。

## 已新增/修改的测试

- 基线 65/65 通过；修复后全量 `mvn.cmd test` 为 73/73 通过。
- 覆盖：legacy request 字段、ontology 参与幂等、嵌套值深拷贝、Provider 规范化、非法策略拒绝、CORS、legacy SQLite 行、exact 1:1 缺目标、Schema 跳号拒绝、契约请求形状。
- `mvn.cmd clean package` 已成功；前端 `npm.cmd run build` 已成功。

## 已完成的运行证据

- 默认 shaded jar smoke：`ENGINE_RUNTIME_RESULT`，workflow 为 `IN_INTERVIEW`，context hash 与 questionnaire detail 成功。
- 新鲜临时 SQLite Admin smoke：先验证未知 `subjects` 字段失败，再添加字段后真实运行 `run-ec7f4d03` 成功；重启同一数据库后恢复 PASSED，models=3、fields=9、ontologyTypes=3、services=3、runs=5。
- 历史 SQLite 临时副本修复后可重启加载；3 条证据不完整运行保持为 legacy，未被伪造为当前运行。
- 契约实验已运行两次：20/20 通过，报告字节一致，SHA-256 为 `98334F6AA88E0A215AB5BE8618B613F39ECC9931F60C2ABD995F05EF8C42A190`。此前报告 manifest 的 source revision 仍是旧 HEAD，需在最终提交后重跑。

## 已完成的文档/论文修订

- `README.md`、`docs/开发进度_v0.1.md`、`docs/架构审计_v0.5.md` 更新为 73 测试及证据边界。
- 新增 `docs/复现审稿报告_v0.1.md`：含复现等级、可信度、主张—实现—证据矩阵、P0-P3 问题与不可声称内容。
- 新增 `docs/论文补充_系统复现与工程验证_v0.1.md`：含可直接入正文的中文段落、表格、伪代码、实验步骤及事实/观察/推断/限制标记。
- 外部论文原稿已补充当前工程复现验证、边界与有效性威胁；历史 37/59 与当前 73/20 已分开。因环境无 XeLaTeX/latexmk，尚未生成 PDF，只完成源文件结构一致性检查。

## 当前已知且不能掩盖的边界

- Provider 仍是进程内 `LocalOntologyProvider`，没有远程/RPC Provider 证据。
- Model 与 ontology 的显式绑定、Replay、权限、HA、性能规模和外部生产业务指标未实现或未验证。
- ontology source multiplicity 仍未强制，只验证 target cardinality。
- Trace/response span 的持久化时点仍有“保存前记录”的工程限制。
- 原始 `data/flexible-engine.db` schema 版本为 4，当前代码目标为 8，且有 legacy runs/旧 Provider 名称；只读审计应报告 WARN，不应改写历史数据制造 PASS。

## 下一步必须完成

1. 检查前一条 commit 是否成功，确认工作区/暂存区状态。
2. 如必要，提交上述已暂存工程和文档修改。
3. 用最终 HEAD 重跑契约实验到 `runs/contract-20/reviewer-20260902-final`，确认 manifest source revision 为最终 commit，20/20 且报告可复核。
4. 对原始 SQLite/JSON 和最终契约报告运行 `scripts/audit_project_state.py`，保留真实 WARN 边界。
5. 执行最终 `git diff --check`、Git 状态检查，并 `git push origin main`。
6. 最终回复必须用中文，列出审稿总评、实际修复、论文补充、测试/实验结果、未解决边界、修改文件、最终 commit hash 与推送状态；外部论文文件需以绝对路径说明且注明不在本仓库提交中。
